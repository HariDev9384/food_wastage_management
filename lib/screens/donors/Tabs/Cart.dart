import 'package:flutter/material.dart';
class DonorCartTab extends StatelessWidget {
  const DonorCartTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Cart Tab'),);
  }
}